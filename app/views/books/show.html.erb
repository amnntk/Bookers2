<%# book の詳細 %>

<% if flash[:success] %>
  <div class="flash">
    <%= flash[:success] %>
  </div>
<% end %>
  <%= render '/users/show', user: @user,book: @booknew %>

  <div class="col-md-8 offset-md-1">
   <h2>Book detail</h2>
    <table class="table table-hover table-inverse">
        <thead>
          <th></th>
          <th></th>
          <th></th>
          <th></th>
          <th></th>
        </thead>
    <tr>
    <td><%= image_tag @user.get_profile_image(100,100) %><br>
     <%= link_to @user.name, user_path(@user.id) %></td>
    <td><%= link_to @book.title, book_path(@book.id) %></td>
    <td><%= @book.body %></td>
    
     <% if @user.id == current_user.id %>
    <td>
      <%#= <a class="btn btn-sm btn-success" href="/books/:id/edit(.:format)" >Edit</a> %>
      <%= link_to "Edit", edit_book_path(@book.id) %></td>
    <td>
      <%#= <a class="btn btn-sm btn-danger" href="book_path(@book.id)", method: :delete, "data-confirm" => "本当に消しますか？" >Destroy</a> %>
      <%= link_to "Destroy", book_path(@book.id) , method: :delete, "data-confirm" => "本当に消しますか？" %></td>
     <% end %>
    </tr>
    </table>
   </div>